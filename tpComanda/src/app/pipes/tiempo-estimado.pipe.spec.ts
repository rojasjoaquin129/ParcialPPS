import { TiempoEstimadoPipe } from './tiempo-estimado.pipe';

describe('TiempoEstimadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoEstimadoPipe();
    expect(pipe).toBeTruthy();
  });
});
